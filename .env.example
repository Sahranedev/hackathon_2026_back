# Copy this file to `.env` and adjust the values if needed.

# PostgreSQL (Docker) credentials — used by docker-compose.yml
POSTGRES_USER="hackasaumon"
POSTGRES_PASSWORD="hackasaumon"
POSTGRES_DB="hackasaumon"
POSTGRES_PORT="5433"

# Connection string used by Prisma. Must match the credentials above.
DATABASE_URL="postgresql://hackasaumon:hackasaumon@localhost:5433/hackasaumon?schema=public"

# JWT
JWT_SECRET="change_me"
JWT_EXPIRES_IN="1d"

STRAVA_CLIENT_ID="your_strava_client_id"
STRAVA_CLIENT_SECRET="your_strava_client_secret"
STRAVA_REDIRECT_URI="http://localhost:3000/api/auth/strava/callback"

# Mistral AI
MISTRAL_API_KEY="your_mistral_api_key"
MISTRAL_MODEL="mistral-small-latest"
MISTRAL_TIMEOUT_MS="60000"

# Demo alerts
# Enable this only during a presentation. The scenario simulates sensor readings
# and creates a slow leak alert without manually calling the tire-sensor API.
DEMO_ALERTS_ENABLED="false"
DEMO_ALERTS_ACTIVITY_DELAY_SECONDS="60"
DEMO_ALERTS_RUNS="1"
DEMO_ALERTS_DEVICE_ID="demo-rear-001"
